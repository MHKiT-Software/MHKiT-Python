name: test_env
channels:
    - defaults
    - conda-forge
dependencies:
    - python=3.7
    - pandas
    - numpy
    - scipy
    - matplotlib
    - requests
    - nose
    - NREL-rex
    - pip:
        - coveralls
